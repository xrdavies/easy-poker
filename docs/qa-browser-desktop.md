# Desktop browser QA

日期：2026-09-13  
目标：`http://localhost:8787/`，桌面视口约 `1280×800`

## 结果

本轮无法完成交互式浏览器 QA：当前会话的浏览器运行时未发现可用实例（`agent.browsers.list()` 返回空数组），因此不能进行页面点击、开桌/加入、操作牌局、截图或读取浏览器控制台与网络面板。没有据此推断 UI 缺陷。

## 已完成的只读检查

- `curl -I --max-time 5 http://localhost:8787/`
- 服务响应：HTTP `200 OK`
- 响应类型：`text/html; charset=utf-8`

## 待补测流程

浏览器实例可用后，使用约 `1280×800` 视口重测：

1. 打开大厅，检查标题、创建/加入入口、列表和空状态。
2. 创建牌桌；另开标签或第二玩家加入。
3. 开始一手牌，执行至少一次 check/call/fold，检查牌面、筹码、操作按钮和状态提示。
4. 检查买入弹窗、结算/摊牌页面及返回大厅流程。
5. 记录截图路径、控制台错误和失败网络请求；按严重程度标记复现步骤。

## 阻塞

- 严重度：阻塞（QA 环境）
- 复现：在本会话初始化浏览器运行时后调用 `agent.browsers.list()`，结果为 `[]`；`getForUrl("http://localhost:8787")` 报告无可用浏览器。
- 处理：需要提供/启用一个可连接的内置浏览器实例后重试。
