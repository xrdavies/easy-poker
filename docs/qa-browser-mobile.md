# Mobile browser QA

日期：2026-09-13  
目标：`http://localhost:8787/`，移动视口约 `390×844`

## 结果（2026-09-13）

MCP 浏览器 B 当前无法启动：配置的持久化目录 `/Users/r001/chrome-profile-B` 已被另一个 Chrome 进程占用，所有 B 工具均返回：

> Browser is already in use for `/Users/r001/chrome-profile-B`, use `--isolated` to run multiple instances of the same browser

因此本 agent 没有绕过锁直接修改浏览器 profile，也没有把另一浏览器的结果冒充为 B 的双玩家测试。此前只读检查确认 `http://localhost:8787/` 返回 `HTTP 200 OK`。

## 可用的移动端证据

同一轮由 MCP 浏览器 A 以 `390×844` 视口打开已存在的牌桌会话，用于补充纯视觉检查：

- Canvas、牌桌、8 个座位、顶部工具栏、底部手牌和状态提示均能绘制。
- 控制台错误数为 `0`；静态图片/音频请求均返回 `200`。
- 顶部工具栏在窄屏下换成两行，但按钮未发生明显重叠或越界。
- 筹码堆在该缩放比例下显示为很小的圆点，面值颜色/花纹不易辨认（严重度：低，建议产品确认是否需要提高移动端最小筹码尺寸）。

这不是完整流程验证：当前会话没有第二个可用浏览器，未能在移动视口独立完成创建/加入、买入弹窗、输入框/滑动条、下注操作和结算/摊牌流转。

## 待补测

解除 profile 锁或提供隔离的 B 实例后，按以下顺序补测并更新本文件：

1. 大厅创建/加入入口及窄屏溢出。
2. 两个玩家加入同桌，完成一手牌的 check/call/fold。
3. 买入弹窗、数字输入框、加注滑动条的触摸命中区域和键盘避让。
4. 摊牌/结算页面及返回大厅。
5. 每个流程记录截图、控制台错误和失败网络请求。
